import { PresenceStatePipe } from './presence-state.pipe';

describe('PresenceStatePipe', () => {
  it('create an instance', () => {
    const pipe = new PresenceStatePipe();
    expect(pipe).toBeTruthy();
  });
});
